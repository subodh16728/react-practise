import { createContext } from "react";
export const UserContext = createContext('sdhbcsjdhbchb');



// When we are creating this file and writing this code, we are basically using a mechanism which react provides to pass a value through the component tree without using the props.